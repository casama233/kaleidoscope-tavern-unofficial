name: temp grasshopper materialize

on:
  push:
    branches:
      - sync/c70eec1-grasshopper

permissions:
  contents: write

jobs:
  materialize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: sync/c70eec1-grasshopper

      - name: Apply locked Grasshopper visual sync
        run: python tools/sync_post12_visuals.py --plan data/upstream/post-1.2/c70eec1/grasshopper/sync-plan.json

      - name: Commit generated runtime outputs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add art/interfaces/asset-registry.json runtime/BP/blocks/cup_grasshopper.json runtime/RP/models/entity/grasshopper.geo.json runtime/RP/textures/kaleidoscope_tavern/block/mixology/grasshopper.png runtime/RP/textures/kaleidoscope_tavern_jar/block/mixology/grasshopper.png
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Materialize Grasshopper post-1.2 sync"
          git push origin HEAD:sync/c70eec1-grasshopper
