# A10 變更與邊界

14 個新輸出：7 杯雞尾酒、6 種吧檯、1 個 Holder。25 個來源（14 JSON、9 PNG、2 mcmeta）逐位元組核實。

此輪未取得完整高腳凳的座墊與渲染模型，因此不把獲知的三元素底座登記成完整凳子；它仍在待辦。沒有增加燈具、香薰或黑板占位。

通用材質接口保留每種物件自己的 render_type、UV 符號、旋轉、rescale 與 shade。動畫用單幀大小。主包沒有玩家手臂動畫或遊戲事件。

本次修正舊回歸測試的適用範圍：A8 的三個動畫只核對原三個，不拒絕新增的兩個；A9 原外觀／編輯檔以批次篩選，仍要求精確原數量；以前作為無效輸入的 Holder／Grasshopper 已轉正例，新的不存在名稱仍必須報錯。所有舊資源本體仍逐位元組比對。
