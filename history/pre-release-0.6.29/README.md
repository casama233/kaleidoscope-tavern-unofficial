# Superseded development material

These generators, interaction simulations and reports are retained for source history and traceability. They are not invoked by the current build or CI. Several generators refer to private development inputs and can overwrite later fixes; use the current root README and tools/build_release.py instead.

The current runtime incorporates the final 0.6.28 shipped assets/scripts plus the 0.6.29 release preparation changes. Private server backups/logs and credentials remain outside this repository.
