name: temp mojito materialize

on:
  push:
    branches:
      - sync/c70eec1-mojito

permissions:
  contents: write

jobs:
  materialize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: sync/c70eec1-mojito

      - name: Apply locked Mojito visual sync
        run: python tools/sync_post12_visuals.py --plan data/upstream/post-1.2/c70eec1/mojito/sync-plan.json

      - name: Commit generated runtime outputs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add art/interfaces/asset-registry.json runtime/BP/blocks/cup_mojito.json runtime/RP/models/entity/mojito.geo.json runtime/RP/textures/kaleidoscope_tavern/block/mixology/mojito.png runtime/RP/textures/kaleidoscope_tavern_jar/block/mixology/mojito.png
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Materialize Mojito post-1.2 sync"
          git push origin HEAD:sync/c70eec1-mojito
